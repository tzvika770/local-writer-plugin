# Local Writer Receiver — release channel

The PUBLIC release channel for the Local Writer WordPress plugin: plugin source,
`update.json` (the version manifest WordPress's native Update URI mechanism reads), and the
release zips. Install/usage docs: `local-writer/README.md`. This repo never contains keys,
credentials, or business data.
